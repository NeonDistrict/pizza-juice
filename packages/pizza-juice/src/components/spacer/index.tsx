import { styled } from '../../system';
import { Flex } from '../flex';

/**
 * Spacer component
 *
 * @description Combine Flex and Spacer to create stackable and responsive layouts.
 */
export const Spacer = styled(Flex, {
  flex: 1,
});
