---
sidebar_position: 1
---

import { Countdown } from '@blockadegames/pizza-juice'

# Countdown

Countdown description.
 

## Import
```jsx
import { Countdown } from '@blockadegames/pizza-juice'
```

## Usage

export const endDate = new Date(new Date().getTime() + 100000).getTime(); // 100 seconds

<Countdown endDate={endDate} />

- - -

```jsx
const endDate = new Date(new Date().getTime() + 100000).getTime() // 100 seconds

<Countdown endDate={endDate} />
```
